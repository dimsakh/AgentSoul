---
name: Декомпозируй «что будет если» на каждой стадии плана
description: При планировании фичи на каждой фазе перебери сценарии жизненного цикла сущности (создание/правка/удаление/отмена/повтор/пауза/гонки/частичный сбой), а не только happy path
type: principle
outcome: error
confidence: 4
impact: 4
confirmed_count: 1
contradicted_count: 0
last_confirmed: 2026-06-21
source_cases:
  - "ProjectA: план заказа выплат клиентом декомпозировал reject/pause/resume/pay, но НЕ рассматривал 'а что если оператор удалит заявку/задачу' → deleteTask не каскадил unreserve → резерв клиента заморожен навсегда (пользователь обнаружил на проде)"
  - "2026-06-21: подтверждение в 2 независимых контекстах — ProjectA «при разработке фич на каждом этапе проводить такую декомпозицию» + ClaudSoul «нужен тасктрекер с декомпозицией … в правильной последовательности весь проект чинить». Новое измерение: декомпозиция повторяема на КАЖДОМ этапе и при заходе в большой проект (последовательность работ), не одноразовый шаг перед стартом"
status: active
modification_history:
  - date: 2026-06-21
    kind: scope_widened
    reason: "второе независимое подтверждение (ProjectA + ClaudSoul); применимость расширена с lifecycle сущности на per-stage декомпозицию по ходу фичи и декомпозицию объёма/последовательности при заходе в большой проект. confidence 3→4, confirmed 1→2"
    trigger_case: "compile-2026-06-21 (session-activity ProjectA_NEW + ClaudSoul)"
fragile: false

# Контекстные якоря
domain: [software_development, planning, financial_systems]
situation: "feature_planning, decompose, refactoring"
trigger: "new_feature_plan, state_machine, lifecycle_entity, balance_operation"
stakes: "data_integrity, frozen_funds, orphan_state, silent_bug"
actors: [agent, interlocutor, system]
environment: multi_session
purpose: new_feature
method: ci_cd
tags: [planning, what_if, edge_cases, lifecycle, state_machine, completeness, decomposition]

# Demand components
need: avoid_regression
urgency: when_relevant
availability: unique

# Связи
related: []
edges:
  - specializes: principle-completeness-over-speed.md
  - similar_to: principle-verify-before-acting.md
---

## Правило

При планировании фичи (особенно в plan-mode / `/decompose`) **на каждой фазе** не ограничивайся «счастливым путём». Для каждой сущности с состоянием перебери сценарии её жизненного цикла и для каждого укажи требуемое поведение + инвариант, который нельзя нарушить:

- **создание** — что резервируется/выделяется?
- **правка** — что переносится при смене ключевых полей (сумма, счёт)?
- **удаление** — что освобождается? кто каскадит? (← чаще всего забывается)
- **отмена / отклонение** — обратный side-effect создания выполнен?
- **повтор / идемпотентность** — двойной вызов не задваивает эффект?
- **пауза / заморозка** — что остаётся held, что снимается?
- **гонки** — два актора одновременно; транзакция перечитывает состояние по id?
- **частичный сбой** — внешний вызов упал на полпути → компенсация/откат?

Особое внимание — **балансовым / резервным операциям**: на каждый side-effect создания (reserve, выделение счёта, блокировка) должен существовать парный обратный эффект (unreserve, возврат), и он обязан срабатывать на **всех** путях выхода из нетерминального состояния, включая **удаление**.

## Почему

Side-effect создания (`reserve`) видно сразу — на него пишут код и тест. Обратный side-effect на «нештатном» выходе (удаление задачи-связки, а не кнопка «Отклонить») — невидимая дыра: фича работает в демо, но в проде оставляет замороженные средства / orphan-состояние. Это не ловится happy-path тестами и всплывает у пользователя.

Симптом-маркер: односторонняя связь (`Task.entityType + entityId` без обратной ссылки) — её удаление НЕ каскадит автоматически; каждый такой линк нужно явно перебрать на «что если удалят родителя/ребёнка».

## Как применять

1. В плане завести секцию **«What-if по стадиям»** — таблица `стадия | состояние ресурса | действие | почему`.
2. Прогнать по ней каждую сущность с состоянием/балансом.
3. Смежная проверка: найти все односторонние entity-task / entity-entity связи и проверить каскады удаления (см. как сделано для `DataChangeRequest` в `deleteTask` — прецедент паттерна).
4. Тест на «удаление в нетерминальном состоянии» — обязателен для балансовых сущностей.
